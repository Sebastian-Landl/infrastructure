services:
  vllm:
    image: avarok/dgx-vllm-nvfp4-kernel:v23
    container_name: ${CONTAINER_NAME:-vllm}
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    environment:
      - MODEL=${VLLM_MODEL:-nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4}
      - PORT=${VLLM_PORT:-8000}
      - HOST=0.0.0.0
      - TENSOR_PARALLEL_SIZE=1
      - MAX_MODEL_LEN=${VLLM_MAX_MODEL_LEN:-262144}
      - GPU_MEMORY_UTIL=${VLLM_GPU_MEMORY_UTILIZATION:-0.80}
      - MAX_NUM_SEQS=128
      - VLLM_EXTRA_ARGS=--served-model-name ${VLLM_SERVED_MODEL_NAME:-nemotron-3-nano}
          --dtype auto
          --swap-space 0
          --kv-cache-dtype ${VLLM_KV_CACHE_DTYPE:-fp8}
          --trust-remote-code
          --enable-chunked-prefill
          --max-cudagraph-capture-size 128
          --async-scheduling
          --mamba-ssm-cache-dtype float16
          --reasoning-parser-plugin ${VLLM_REASONING_PARSER_PLUGIN:-/app/reasoning_parsers/nano_v3_reasoning_parser.py}
          --reasoning-parser ${VLLM_REASONING_PARSER:-nano_v3}
          --enable-auto-tool-choice
          --tool-call-parser ${VLLM_TOOL_CALL_PARSER:-qwen3_coder}
          --VLLM_NVFP4_GEMM_BACKEND=marlin
    volumes:
      - ${HF_CACHE_DIR:-~/.cache/huggingface}:/root/.cache/huggingface
      - ./reasoning_parsers:/app/reasoning_parsers
    ports:
      - "${VLLM_PORT:-8000}:${VLLM_PORT:-8000}"
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
