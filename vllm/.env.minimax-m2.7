# =============================================================================
# saricles/MiniMax-M2.7-REAP-172B-A10B-NVFP4-GB10 — text / reasoning / tool-calling
# Usage: docker compose --env-file .env.qwen3.6 up -d
#
# Note: Model card says "compatible and tested against vllm main".
#       Use vllm/vllm-openai:v0.20.2 (latest stable; no cu130 variant exists).
#
# Model card: saricles/MiniMax-M2.7-REAP-172B-A10B-NVFP4-GB10
# Max supported context: 196608 tokens.
# =============================================================================

# --- Model ---
VLLM_MODEL=saricles/MiniMax-M2.7-REAP-172B-A10B-NVFP4-GB10
VLLM_SERVED_MODEL_NAME=vllm
CONTAINER_NAME=vllm

# --- Reasoning / tool calling ---
VLLM_REASONING_PARSER=minimax_m2
VLLM_TOOL_CALL_PARSER=minimax_m2

# --- Attention / MoE backend ---
VLLM_ATTENTION_BACKEND=flashinfer
VLLM_MOE_BACKEND=marlin
# flashinfer_cutlass is recommended by the model card for this NVFP4 MoE model.
VLLM_TEST_FORCE_FP8_MARLIN=1
#VLLM_QUANTIZATION=compressed-tensors
#VLLM_LINEAR_BACKEND=cutlass
COMPILATION_CONFIG_FLAG=--compilation-config '{"cudagraph_mode":"none","inductor_compile_config":{"combo_kernels":false,"benchmark_combo_kernel":false,"max_autotune":false,"max_autotune_gemm":false}}'

# --- Speculative decoding (MTP) ---
#VLLM_SPECULATIVE_CONFIG_FLAG=--speculative-config '{"method": "mtp", "num_speculative_tokens": 3}'

# --- Context / memory ---
VLLM_MAX_MODEL_LEN=50000
VLLM_MAX_NUM_SEQS=16
VLLM_MAX_NUM_BATCHED_TOKENS=16384
#VLLM_MAX_NUM_BATCHED_TOKENS=8192
VLLM_GPU_MEMORY_UTILIZATION=0.85

# --- KV cache ---
VLLM_KV_CACHE_DTYPE=fp8_e4m3
# bfloat16 is correct for NVFP4 on Ampere+; float16 is narrower dynamic range
VLLM_DTYPE=bfloat16

# --- Multimodal flags (image + video; no audio) ---
VLLM_INSTALL_AUDIO_DEPS=0
#VLLM_VIDEO_PRUNING_FLAG=--video-pruning-rate 0.5
# num_frames=256 for 80 GB GPUs; reduce to 64-128 for ≤40 GB
#VLLM_MEDIA_IO_KWARGS_FLAG=--media-io-kwargs '{"video": {"fps": 2, "num_frames": 256}}'
#VLLM_ALLOWED_LOCAL_MEDIA_FLAG=--allowed-local-media-path /

# --- HF / infra ---
HF_CACHE_DIR=~/.cache/huggingface
HF_TOKEN=
