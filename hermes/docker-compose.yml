services:
  hermes:
    image: nousresearch/hermes-agent:latest
    container_name: hermes
    restart: unless-stopped
    command: gateway run
    ports:
      - '8642:8642'
    volumes:
      - ${HERMES_DATA:-hermes-data}:/opt/data
