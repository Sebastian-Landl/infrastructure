services:
  watchtower:
    image: containrrr/watchtower:1.7.1
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: --label-enable --schedule "${WATCHTOWER_SCHEDULE}" --cleanup
    restart: unless-stopped